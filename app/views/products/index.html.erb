<p>products</p>
<p>test</p>
<div class="container">
    <div class="row">
        <div class="col-sm-12">
            <table class="table">
                <tr>
                    <th>ID</th>
                    <th>URL</th>
                    <th>Description</th>
                </tr>
                <% @products.each do |product|%>
                    <tr>
                        <td><%= product.id%></td>
                        <td><%= product.name %></td>
                        <td><%= product.url%></td>
                        <td><%= product.description%></td>
                    </tr>
                <%end%>
            </table>
        </div>
    </div>
</div>


